//Functions are piece of code that does one or more actions. 
//We use functions because we do not want to repeat ourselves. We want to DRY. DO NOT REPEAT YOURSELF.

/*function go(name, age){ //you put in arguments so that you can reuse the code. 
// 	alert(name);
// 	alert(age);
// }
//the only way to use the go function is by calling it. 
// go('Dan', 30);
 go('Moi', 32);
*/


//Functions also return a value
/*
function add(first, second){
	return first+second;
}
alert(add(1,3));
*/

//Remember EVERY function will return undefined if you do not return something. 


/*var myList = ['apples', 'oranges', 'bananas'];
myList[3] = 'pineapples';

//Loops
var times = 0;
while(times < 10){
	console.log('tried it', times);
	times = times+1;
}

for (var i = 0; i < 10; i++) {
	console.log('i is', i);
};
*/